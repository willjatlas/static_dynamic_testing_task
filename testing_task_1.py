class CardGame:

""" needs double equals in equality check, else 
    needs semi colon """
  def check_for_ace(self, card):
    #if card.value = 1:
      return True
    #else
      return False
   
""" def, not dif, no comma seperating card args 
    wrong card arg in first return, incorrect 
    indentation below if statement"""
  # dif highest_card(self, card1 card2):
  #if card1.value > card2.value:
    #return card
  #else:
    #return card2
  

""" wrong indentation(whole method), total is not assigned """
#def cards_total(self, cards):
  #total
  # for card in cards:
  #   total += card.value
  #   return "You have a total of" + total